

2025-08-22 FRI

From hello-world menuconfig::

	Name: CLOCK_STM32_MCO
	Type: bool
	Value: n

	Help:

	  Allows to output various different clock sources onto the MCO pin
	  using a configurable prescaler.

	Direct dependencies (=n):
	     DT_HAS_ST_STM32_CLOCK_MCO_ENABLED(=n) || DT_HAS_ST_STM32F1_CLOCK_MCO_ENABLED(=n)  (=n)
	  && CLOCK_CONTROL_STM32_CUBE(=y)
	  && CLOCK_CONTROL(=y)

	Default:
	  - y

. . . It looks like we do not have a clock related on chip facility enabled.

On further readings maybe this is not what we need:

https://docs.zephyrproject.org/latest/build/dts/api/bindings/clock/st%2Cstm32-clock-mco.html

Menuconfig says we have these STM32 clock symbols enabled::

	CLOCK_STM32_HSE_CLOCK(=8000000) "HSE clock value"
	CLOCK_STM32_MCO(=n)
	CLOCK_STM32_MUX(=n) "STM32 clock mux driver"
	PTP_CLOCK_STM32_HAL(=n) "STM32 HAL PTP clock driver support"
